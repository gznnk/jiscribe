/**
 * Playwright configuration entry (`@jiscribe/canvas-sdk/testing/playwright-config`), for
 * a plugin's `playwright.config.ts` and nothing else.
 */

export * from "@jiscribe/canvas/testing/playwright-config";
