/**
 * Harness dev-server configuration entry
 * (`@jiscribe/canvas-sdk/testing/vite-config`), for a plugin's harness
 * `vite.config.ts`. Start that harness with `vite --configLoader runner`; see the
 * canvas entry this re-exports for why.
 */

export * from "@jiscribe/canvas/testing/vite-config";
