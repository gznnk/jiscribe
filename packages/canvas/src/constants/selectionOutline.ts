/**
 * Stroke width (world units, so it scales with the zoom like the shape it frames)
 * shared by every selection outline: the object's own frame (Outline) and the text
 * slot addressed inside it (TextSlotOutline).
 */
export const SELECTION_OUTLINE_WIDTH = 1.5;
