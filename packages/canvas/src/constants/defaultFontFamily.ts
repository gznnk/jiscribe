/**
 * Default font applied to newly created shapes and used as the fallback when
 * a doc/label has no explicit fontFamily. Hosts can override the default for
 * new shapes via the Canvas `theme` prop (`CanvasTheme.fontFamily`); this
 * constant is the built-in value shared by the doc defaults and the theme
 * presets.
 */
export const DEFAULT_FONT_FAMILY = "Noto Sans JP";
