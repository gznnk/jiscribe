import { CROSS_DOC_DEFAULTS } from "./CrossDoc";
import { createFrameShapeFactory } from "../../utils/createFrameShapeFactory";

/** Factory for creating Cross shapes (Frame-family shared logic generated from defaults). */
export const CrossShapeFactory = createFrameShapeFactory(CROSS_DOC_DEFAULTS);
