import { DELAY_DOC_DEFAULTS } from "./DelayDoc";
import { createFrameShapeFactory } from "../../utils/createFrameShapeFactory";

/** Factory for creating Delay shapes (Frame-family shared logic generated from defaults). */
export const DelayShapeFactory = createFrameShapeFactory(DELAY_DOC_DEFAULTS);
