import { CLOUD_DOC_DEFAULTS } from "./CloudDoc";
import { createFrameShapeFactory } from "../../utils/createFrameShapeFactory";

/** Factory for creating Cloud shapes (Frame-family shared logic generated from defaults). */
export const CloudShapeFactory = createFrameShapeFactory(CLOUD_DOC_DEFAULTS);
