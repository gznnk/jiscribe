{
	"comment": "Scopes build/ as ESM on its own. generateFontsCss.ts is a .ts file node itself runs (generate:fonts / check:fonts), and without a package.json naming the module type node reparses it and warns on every run. Kept out of the package root so that playwright's CommonJS transpilation of the e2e config, which resolves against the root, is unaffected.",
	"type": "module"
}
