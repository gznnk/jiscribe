# The Jiscribe file format

How a Jiscribe diagram is stored, for a reader who has decided to read or write one of these files directly.
The field-level specification lives in the JSON Schema beside it, [`jiscribe.schema.json`](./jiscribe.schema.json): look up a type's `$def` there for its exact properties, defaults and constraints.
