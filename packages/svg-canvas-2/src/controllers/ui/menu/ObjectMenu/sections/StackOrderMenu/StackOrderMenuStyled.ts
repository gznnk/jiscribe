import styled from "@emotion/styled";

export const StackOrderMenuRow = styled.div`
	display: flex;
	flex-direction: row;
	gap: 4px;
	padding: 4px;
	background-color: #ffffff;
	border-radius: 8px;
	box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
	pointer-events: auto;
`;
