import { createFrameObjectFactory } from "@workspace/canvas-sdk/doc";

import { FILE_DOC_DEFAULTS } from "./FileDoc";

/** Factory for creating File shapes (Frame-family shared logic generated from defaults). */
export const FileObjectFactory = createFrameObjectFactory(FILE_DOC_DEFAULTS);
