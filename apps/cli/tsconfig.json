{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		"types": ["node"],
		// The render harness (harness/) is a browser page: it is the only JSX here,
		// and the only thing that touches the DOM.
		"jsx": "react-jsx"
	},
	"include": ["src", "harness", "render-tests"]
}
